/*
if($_SERVER['HTTP_HOST'] != 'YOUR_DOMAIN')
{
    header('location: '.SERVER.'');
    exit;
}
*/

// delete the /* and the */ if you not use localhost.


dont forget to change if going live (index.php)